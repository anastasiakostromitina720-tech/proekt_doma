# -----------------------------------------------------------------------------
# Root .env — consumed ONLY by docker-compose.yml
# Copy to `.env` at the repo root to customize docker-compose defaults.
# Do NOT put application secrets here — see apps/api/.env and apps/web/.env.local.
# -----------------------------------------------------------------------------

POSTGRES_USER=proekt
POSTGRES_PASSWORD=proekt
POSTGRES_DB=proekt_doma
POSTGRES_PORT=5432

REDIS_PORT=6379

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_BUCKET=proekt-doma
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
